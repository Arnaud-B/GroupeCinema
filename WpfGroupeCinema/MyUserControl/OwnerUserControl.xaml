<baseUserControl:BaseUserControl  
        x:Class="WpfGroupeCinema.CinemaListUserControl.OwnerUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfGroupeCinema.CinemaListUserControl"
        xmlns:baseUserControl="clr-namespace:WpfGroupeCinema.CinemaListUserControl"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="ownerIdTxtB" Text="{Binding Owner.Id}" Grid.Row="0" Grid.Column="0"/>
        <TextBlock x:Name="ownerFirstnameTxtB" Text="{Binding Owner.Firstname}" Grid.Row="0" Grid.Column="1"/>
        <TextBlock x:Name="ownerLastnameTxtB" Text="{Binding Owner.Lastname}" Grid.Row="0" Grid.Column="2"/>
      </Grid>
</baseUserControl:BaseUserControl>
